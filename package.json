{
  "name": "bdct-js",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "clean:pacts": "node -e \"require('fs').rmSync('pacts',{recursive:true,force:true})\"",

    "pact:consumer": "npm run clean:pacts && cross-env NODE_OPTIONS=--experimental-vm-modules jest src/consumer/consumer.pact.test.js",
    "pact:publish": "node scripts/publish.cjs",
    "pact:can-i-deploy": "node scripts/can-i-deploy.cjs",

    "provider:start": "node src/provider/server.js",
    "provider:publish-contract": "node scripts/publish-provider.cjs"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^15.0.1",
    "@pact-foundation/pact-cli": "^16.1.2",
    "cross-env": "^7.0.3",
    "jest": "^30.0.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true,
    "transform": {}
  }
}
